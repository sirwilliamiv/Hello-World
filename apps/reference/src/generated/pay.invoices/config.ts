// ┌───────────────────────────────────────────────────────────────────┐
// │ GENERATED BY FORGE — DO NOT EDIT                                   │
// │ capability: pay.invoices@1.0.0                                    │
// │ template:   config.ts@1.0.0                                       │
// │ Edits are reported by `forge drift` and will conflict on upgrade.  │
// │ customise via the lineItemFormatting, numberingScheme, paymentTerms slot(s) instead│
// └───────────────────────────────────────────────────────────────────┘

import { configureInvoices } from '@forge/pay-invoices'
import * as slots from '@/slots/pay.invoices'

configureInvoices({
  // Numbering is gapless and sequential PER LEGAL ENTITY. The sequence is
  // allocated inside the insert transaction, so a rolled-back invoice does not
  // burn a number and two concurrent inserts cannot share one.
  legalEntity: "Reference Client LLC",
  numberingScheme: "INV-{YYYY}-{SEQ:5}",
  paymentTermsDays: 30,
  overdueCheckCron: "0 6 * * *",
  slots,
})
