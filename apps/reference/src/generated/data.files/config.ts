// ┌───────────────────────────────────────────────────────────────────┐
// │ GENERATED BY FORGE — DO NOT EDIT                                   │
// │ capability: data.files@1.0.0                                      │
// │ template:   config.ts@1.0.0                                       │
// │ Edits are reported by `forge drift` and will conflict on upgrade.  │
// │ customise via the allowedTypes, processingPipeline, retentionRules, sizeLimits slot(s) instead│
// └───────────────────────────────────────────────────────────────────┘

import { configureFiles } from '@forge/data-files'
import { env } from '@/generated/env'
import * as slots from '@/slots/data.files'

configureFiles({
  driver: "local",
  bucket: env.STORAGE_BUCKET,
  credentials: env.STORAGE_CREDENTIALS,
  maxSizeBytes: 2.62144e+07,
  allowedContentTypes: ["image/jpeg","image/png","image/heic","application/pdf"],
  // A file is quarantined until it clears scanning; file.uploaded is published
  // only after it does, so no consumer ever sees an unscanned object.
  scanPolicy: "require_clean",
  slots,
})
