// ┌───────────────────────────────────────────────────────────────────┐
// │ GENERATED BY FORGE — DO NOT EDIT                                   │
// │ capability: docs.generation@1.0.0                                 │
// │ template:   config.ts@1.0.0                                       │
// │ Edits are reported by `forge drift` and will conflict on upgrade.  │
// │ customise via the brandingOverride, outputFormats, templateHelpers slot(s) instead│
// └───────────────────────────────────────────────────────────────────┘

import { configureDocuments } from '@forge/docs-generation'
import * as slots from '@/slots/docs.generation'

configureDocuments({
  defaultFormat: "pdf",
  pageSize: "letter",
  pdfEngine: "chromium",
  // Templates consume kernel.ui tokens, so documents match the product's
  // branding without separate design work.
  tokensHref: '/generated/kernel.ui/tokens.css',
  slots,
})
