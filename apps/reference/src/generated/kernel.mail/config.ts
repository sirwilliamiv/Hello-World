// ┌───────────────────────────────────────────────────────────────────┐
// │ GENERATED BY FORGE — DO NOT EDIT                                   │
// │ capability: kernel.mail@1.0.0                                     │
// │ template:   mail-config.ts@1.0.0                                  │
// │ Edits are reported by `forge drift` and will conflict on upgrade.  │
// │ eject it with `forge eject` if it must diverge                    │
// └───────────────────────────────────────────────────────────────────┘

import { configureMail } from '@forge/kernel-mail'
import { env } from '@/generated/env'

// Values are read from the environment at runtime. Forge generates the schema
// for these names; it never generates or stores the values.
configureMail({
  dsn: env.MAIL_DSN,
  from: env.MAIL_FROM_ADDRESS,
})
