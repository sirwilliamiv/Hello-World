{
  "$schema": "../../schemas/capability.schema.json",
  "spec_version": 1,
  "id": "kernel.observability",
  "version": "1.0.0",
  "kernel_range": "^1.0.0",
  "name": "Errors, logs, metrics, health",
  "tier": "kernel",
  "price": {
    "model": "included"
  },
  "integration_weight": 0,
  "summary": "Structured logging, error capture, tracing, health, and metrics. Present in every product because a client whose application is down is the only situation in which every other guarantee in this system stops mattering. The per-client smoke tests generated by other capabilities are promoted into synthetic monitoring here from Phase 5.",
  "package": {
    "name": "@forge/kernel-observability"
  },
  "requires": [],
  "consumes": [
    {
      "name": "**",
      "contract_versions": [
        1
      ],
      "required": false,
      "reason": "sample every event in the system for tracing and metrics",
      "handler": "sampleEvent"
    }
  ],
  "exposes": [
    {
      "kind": "interface",
      "name": "logger",
      "signature": "logger.info|warn|error(msg: string, fields?: Record<string, unknown>): void",
      "description": "Structured only. Values bound to a declared secret name are redacted by type before emission, never by pattern matching on key names."
    },
    {
      "kind": "interface",
      "name": "captureError",
      "signature": "captureError(err: unknown, context?: Record<string, unknown>): void"
    },
    {
      "kind": "route",
      "name": "health",
      "path": "/health",
      "description": "Liveness and readiness, including database and queue reachability."
    },
    {
      "kind": "route",
      "name": "metrics",
      "path": "/metrics"
    },
    {
      "kind": "registry",
      "name": "healthChecks",
      "description": "Liveness probes contributed by whichever capabilities own external resources. This capability declares no dependencies on purpose — an observability layer that imported the data layer and the queue would be unable to report on a product that lacks either. The dependency is inverted: contributors register, and a required check that nobody registered reports unknown and fails the probe rather than passing silently."
    },
    {
      "kind": "interface",
      "name": "registerHealthCheck",
      "signature": "registerHealthCheck(check: { name: string; check: () => Promise<HealthStatus> }): void"
    }
  ],
  "external": [
    {
      "service": "Error tracking provider",
      "purpose": "Aggregate captured exceptions across the fleet.",
      "optional": true,
      "credentials": [
        {
          "name": "ERROR_TRACKING_DSN",
          "description": "Ingest endpoint for error capture.",
          "scope": "environment",
          "optional": true
        }
      ]
    },
    {
      "service": "Metrics provider",
      "purpose": "Scrape or receive application metrics.",
      "optional": true,
      "credentials": [
        {
          "name": "METRICS_ENDPOINT",
          "description": "Metrics ingest endpoint.",
          "scope": "environment",
          "optional": true
        }
      ]
    }
  ],
  "tests": {
    "smoke": [
      {
        "name": "health endpoint reports ready",
        "asserts": "/health returns 200 with database and queue reachable"
      },
      {
        "name": "secrets are not logged",
        "asserts": "a log call carrying a value bound to a declared credential emits a redacted field"
      }
    ]
  },
  "migrations": [],
  "templates": [
    {
      "id": "instrumentation.ts",
      "version": "1.0.0",
      "output": "src/instrumentation.ts",
      "zone": "managed",
      "description": "Next.js instrumentation hook. Wires tracing and error capture at process start."
    },
    {
      "id": "env.ts",
      "version": "1.0.0",
      "output": "src/generated/env.ts",
      "zone": "managed",
      "description": "The environment variable schema for every credential declared across the resolved graph. Fails fast at boot on a missing variable. Contains names and types only, never values.",
      "scope": "graph"
    }
  ]
}
